apiVersion: v1
kind: Service
metadata:
  name: whatsapp-backend-service
  labels:
    app: whatsapp-agent
    component: backend
spec:
  type: ClusterIP
  selector:
    app: whatsapp-agent
    component: backend
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
